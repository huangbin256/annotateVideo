@import "../pcss/utils.pcss";

.HomeView{
	
	flex: 1;
	display: flex;
	flex-direction: row;
	flex-wrap: wrap;
	align-content: flex-start;
	align-items: center;
	
	.video-con{
		flex: 1;
		display: flex;
		align-items: center;
		justify-content: center;
	}
}